<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- 추가한 부분 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ 알림 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission
        android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        <!-- 추가한 부분 -->
        <activity android:name=".list.ui.PillListActivity" />
        <activity android:name=".list.ui.AddPillActivity" />

        <!-- 알림 화면 등록 -->
        <activity android:name=".alarm.ui.AlarmListActivity" />
        <activity android:name=".alarm.ui.AddAlarmActivity" />

        <!-- 알림 수신 리시버 -->
        <receiver
        android:name=".alarm.AlarmReceiver"
        android:exported="false" />

        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medicell">
        <activity
            android:name=".SquareCamera"
            android:exported="false" />
        <activity
            android:name=".CameraView"
            android:exported="false" />
        <activity
            android:name=".threeButton"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Medicell">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
